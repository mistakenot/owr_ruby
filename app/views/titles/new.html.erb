<%= form_for :title, url: titles_path do |f| %>

  <% if @title.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@title.errors.count, "error") %> prohibited
        this title from being saved:
      </h2>
      <ul>
        <% @title.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', titles_path %>
